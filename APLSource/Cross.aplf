 Cross←{
     folds←(⍵-1)⌽⍺                    ⍝ ts tr1 tr2 ... tr(n-1)
     Model←Euclidean KNN (↑⍪⌿1↓folds) ⍝ model with tr folds
     {⍵∘Model Accuracy ⊢⌿↑1↑folds}¨ks ⍝ accuracy for ts & each ks
 }
