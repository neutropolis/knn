 Main←{
     ⍝ preparing data
     Path←'/Users/j/Downloads/iris.csv'
     Data←⊃⎕CSV Path''(2 2 2 2 1)1
     Data[;Lst]←(∪⍳⊢)Data[;Lst←⊃⌽⍴Data]
     ⍝ training
     (Tr V Ts)←Data Split 8 1 1
     Model←Euclidean KNN Tr
     ⍝ validation
     (Xv Yv)←XY V
     Ks←¯1+2×⍳8
     Val←Ks∘.Model↓Xv
     Hamming←+/≠
     Ham←(⊂,Yv)Hamming¨↓Val ⍝ Need to automate choosing K
     ⍝ test
     (Xt Yt)←XY Ts
     Model←5∘Model
     Ham←(,Yt)Hamming Model¨↓Xt
     Ham÷≢Yt
 }
